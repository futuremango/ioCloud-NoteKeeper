import { createContext } from "react";
const noteContext = createContext();
export default noteContext; //only meant to be read nd used, noteContext
// and noteState can be use in the same file just do this for better reusibility